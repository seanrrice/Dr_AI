# Doctor AI — unified Python environment (transcription + face emotion + gait)
#
# Quick start (repo root):
#   py -3.11 -m venv .venv_unified
#   .\.venv_unified\Scripts\Activate.ps1
#   pip install -U pip
#   pip install -r requirements-unified.txt
#
# Python: 3.11 recommended (matches Mediapipe / RealSense wheels commonly available).
#
# Why these pins:
#   - mediapipe==0.10.21 (gait pose + emotion face mesh) expects protobuf 4.x; protobuf 6.x
#     breaks classic MediaPipe protobuf usage (see Gait/requirements.txt).
#   - Gait capture historically used numpy<2; numpy 1.26.x stays compatible with torch/pandas here.
#   - Training-only stack (Jupyter, CUDA-specific torch, etc.) is NOT included — use
#     emotion_pipeline/requirements-train.txt in a separate venv if you train models.
#
# Optional / manual:
#   - pyrealsense2: wheel must match your Intel RealSense SDK + OS + Python version.
#   - GPU torch: replace torch/torchvision lines with CUDA builds from pytorch.org if needed.
#   - Whisper models download on first use (network).

# --- Pin shared transitive deps first (resolver honors order; explicit pins win) ---
numpy==1.26.4
protobuf==4.25.8

# --- Web servers (transcription :5000, gait API :8000) ---
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
python-socketio==5.10.0

# --- Speech / transcription (DrAITranscription) ---
whisperlivekit==0.2.20
python-multipart
websockets>=13.0
sounddevice==0.5.5
av==16.0.1
coloredlogs==15.0.1
humanfriendly==10.0
tokenizers==0.22.1
huggingface_hub==1.1.4
hf-xet==1.2.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.12.1
fsspec==2025.10.0
filelock==3.20.0
tqdm==4.67.1
typer-slim==0.20.0
shellingham==1.5.4
click==8.3.0
sniffio==1.3.1
idna==3.11
certifi==2025.11.12
PyYAML==6.0.3
packaging==25.0
setuptools==80.9.0
wheel==0.45.1
typing_extensions==4.15.0
sympy==1.14.0
mpmath==1.3.0
flatbuffers==25.9.23
cffi==2.0.0
pycparser==2.23
colorama==0.4.6
pyreadline3==3.5.4

# --- High-quality audio resample (optional path in DrAITranscription/app.py) ---
scipy==1.11.4

# --- Computer vision / pose / depth (Gait + emotion webcam) ---
# jax/jaxlib: mediapipe declares jax; explicit pins avoid long pip backtracking on Windows.
jax==0.6.1
jaxlib==0.6.1
mediapipe==0.10.21
# OpenCV: `import cv2` — PyPI package name is opencv-python (not “cv2”).
# Do not also list opencv-contrib-python; the two wheels conflict. Contrib extras are unused in this repo.
opencv-python==4.11.0.86
pyrealsense2

# --- Emotion inference (emotion_pipeline runtime; used by webcam + spec logger) ---
torch==2.9.0
torchvision==0.24.0
Pillow==12.0.0
pandas==2.3.3
matplotlib==3.10.7
